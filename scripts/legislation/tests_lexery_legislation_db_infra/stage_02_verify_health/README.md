# stage_02_verify_health (placeholder)

**Навіщо:** тести verify + sync_health (Supabase/R2/Qdrant).  
**Що буде:** кейси для `verify --nreg`, `--write-health`.  
**Коли:** при формалізації stage-based тестів.
