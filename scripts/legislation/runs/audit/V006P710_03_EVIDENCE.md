# Evidence: v006p710-03 (Рішення КСУ)

## Документ
- **nreg**: `v006p710-03`
- **title**: Рішення Конституційного Суду України у справі за конституційним поданням 73 народних депутатів України щодо відповідності Конституції України (конституційності) здійсненого Президентом України права вето стосовно прийнятого Верховною Радою України Закону України "Про внесення змін до статті 98 Конституції України" та пропозицій до нього (справа щодо права вето на закон про внесення змін до Конституції України)
- **canonical_typ**: `22` (Рішення КСУ)
- **canonical_organs**: `79:20030311:6-рп/2003`
- **expected_chunks**: 25

## Raw Text Prefix (snippet200)
```
ІМЕНЕМ УКРАЇНИ
РІШЕННЯ КОНСТИТУЦІЙНОГО СУДУ УКРАЇНИ
Рішення Конституційного Суду України у справі за конституційним поданням 73 народних депутатів України щодо відповідності Конституції України (конституційності) здійсненого Президентом України права вето стосовно прийнятого Верховною Радою України Закону України "Про внесення змін до статті 98 Конституції України" та пропозицій до нього (справа щ
```

## Summary Prefix
```
Рішення КСУ визнає конституційним право вето Президента України щодо Закону України 'Про внесення змін до статті 98 Конс
```

## Класифікація
- **Expected**: `ccu_decision` (Рішення КСУ)
- **Actual**: `ccu_decision` ✅
- **Predicted**: `ccu_decision` ✅
- **Predicted Reason**: `typ=22, title/summary/snippet/raw_txt indicates CCU decision`
- **Confidence**: `high`

## Root Cause Analysis

### Проблема
Документ правильно класифікований, але виявлено дві проблеми в системі:

1. **`buildCanonical.ts` не передавав `raw_txt` до `enrichDocumentType`**
   - Під час імпорту нових документів система не використовувала `raw_txt` для priority ladder
   - Це могло призвести до неправильної класифікації для документів, де сигнали є тільки в `raw_txt`

2. **Правило для `typ=22` перевіряло тільки `title` і `summary`**
   - Не перевіряло `snippet` або `raw_txt` для виявлення сигналів КСУ
   - Це могло призвести до проблем, якщо в title немає явних сигналів КСУ, але вони є в `snippet`/`raw_txt`

### Виправлення

1. **`buildCanonical.ts`**: Додано передачу `raw_txt` до `enrichDocumentType`
   ```typescript
   const rawTxt = txtContent ? txtContent.substring(0, 800) : undefined;
   const docTypeEnrichment = await enrichDocumentType({
     // ... інші параметри
     raw_txt: rawTxt,  // PRIORITY LADDER: raw_txt має найвищий пріоритет
   });
   ```

2. **`guessDocumentTypeV2.ts`**: Покращено правило для `typ=22`
   - Тепер перевіряє `raw_txt`, `snippet`, `summary` і `title` в пріоритетному порядку
   - Використовує priority ladder: `raw_txt > snippet > summary > title`
   - Більш надійне виявлення сигналів КСУ навіть якщо в title є згадка про інші органи (наприклад, Президента)

3. **Cache version**: Оновлено до `doc_type_v7` для інвалідації старих кешованих результатів

## Результат
- ✅ Документ правильно класифікований як `ccu_decision`
- ✅ Система тепер правильно обробляє рішення КСУ навіть якщо в title є згадка про інші органи
- ✅ Під час імпорту нових документів `raw_txt` передається до `enrichDocumentType`
- ✅ Правило для `typ=22` тепер перевіряє всі джерела сигналів (raw_txt, snippet, summary, title)

## Перевірка
- **Verify**: PASS: 22 / FAIL: 0 ✅
- **Audit**: severity: OK, predicted_slug: `ccu_decision` ✅
- **Qdrant**: payload синхронізовано (25 chunks + 1 act) ✅
